---
en:
  activemodel:
    attributes:
      custom_csv:
        columns: Columns
        file: File
        remove_all: Remove all current census data
    errors:
      models:
        custom_csv:
          attributes:
            columns:
              blank_name: Column %{index} must have a name
              invalid_type: 'Column %{index} has invalid type: %{type}'
              voters_exist: Cannot change configuration while census data exists.
                Remove existing data first.
            file:
              columns_not_configured: Please configure columns first
              extra_columns: 'Unexpected columns in CSV: %{columns}. Please use only
                the configured columns.'
              malformed_csv: 'The CSV file is malformed: %{message}'
              missing_columns: 'Missing columns in CSV: %{columns}'
              validation_error: 'Row %{row}, column ''%{column}'': %{error}'
              voters_exist: Cannot upload file while census data exists. Remove existing
                data first.
  decidim:
    admin:
      menu:
        elections_menu:
          census_updates: Census updates
    elections:
      admin:
        census:
          update:
            success_config_only: Census configuration saved. Please upload a CSV file
              to complete the setup.
        census_updates:
          create:
            error: There was an error creating the entry
            success: Entry created successfully
          destroy:
            success: Entry deleted successfully
          errors:
            already_exists: An entry with this data already exists
            blank_column: "%{column} cannot be blank"
          index:
            actions: Actions
            clear: Clear
            confirm_delete: Are you sure you want to delete this entry?
            created_at: Created at
            delete: Delete
            entries_count:
              one: 1 unique entry
              other: "%{count} unique entries"
            import_from_survey: Import from survey
            new_entry: New entry
            search: Search
            search_placeholder: Search...
            title: Census Status
            user_identifier: User identifier
          new:
            cancel: Cancel
            create: Create entry
            title: New Census Entry
        censuses:
          column_row:
            column_name: 'Column name:'
            column_type: 'Column type:'
            delete: Delete
          custom_csv_form:
            cancel: Cancel
            census_attributes: Census attributes
            column_types:
              alphanumeric: A-Z 0-9 only text
              date: Date
              free_text: Free text input
              number: Number
              text_trim: Text (trim spaces)
            current_configuration: Current configuration
            description: Built your own census with custom attributes. The main requirement
              is that the combination of attributes must be unique (no 2 rows can
              have the exact combination)
            edit_columns: Edit columns
            new_column: "+ New column"
            remove_before_upload: If you want to upload a new census or change the
              configuration, you must first remove the existing data.
            save_configuration: Save configuration
            upload_description_html: Must be a file in CSV format with the columns
              defined above. It must be separated with a semicolon (;)
            upload_file: Upload file
            upload_title: Upload a CSV file
        survey_imports:
          create:
            error: Could not save configuration
            success: Survey import configuration saved successfully
          errors:
            no_entries_selected: Please select at least one entry to import
            question_not_found: One or more mapped questions were not found
            unmapped_column: "Column '%{column}' must be mapped to a question"
          import:
            error: Could not import entries
            no_valid_entries: No valid entries to import
            success: Import of %{count} entries has been queued and will be processed
              in background
          index:
            back_to_census: Back to census
            cancel: Cancel
            change_survey: Change survey
            import_count:
              one: Import 1 entry
              other: Import %{count} entries
            next: Next
            no_responses: No responses found in the selected survey
            no_valid_entries: No valid entries to import. All responses are either
              incomplete or already exist in the census.
            previous: Previous
            ready_to_import: Ready to import
            skipped: Skipped
            title: Import from Survey
          new:
            cancel: Cancel
            census_column: Census column
            field_mapping: Field mapping
            field_mapping_description: Map each census column to a corresponding survey
              question
            no_columns_warning: Please configure census columns first before setting
              up survey import
            no_surveys_warning: No survey components found in this participatory space.
              Please create a survey first.
            save_configuration: Save configuration
            select_component: Select a survey component
            select_question: Select a question
            select_survey: Select a survey
            survey: Survey
            survey_component: Survey component
            survey_question: Survey question
            title: Configure Survey Import
      censuses:
        custom_csv:
          label: Custom CSV
        custom_csv_form:
          invalid: The provided data does not match any registered voter
